import styled from "styled-components"
import { Nav } from "../components/Nav"

export function History(): JSX.Element {
  return (
    <>
      <Nav icon="chevron" handleClick={() => {}} title="Lesson History" />
    </>
  )
}
